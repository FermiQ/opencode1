# `infra/app.ts`

## Overview

This file defines the cloud infrastructure for the OpenCode application using SST (Serverless Stack Toolkit) for Cloudflare. It sets up domains, a Cloudflare Worker for the API, an R2 bucket for storage, and an Astro site for the web frontend.

## Key Components

*   **`domain` (constant):** Determines the application's domain based on the deployment stage (`production`, `dev`, or other stages).
*   **`bucket` (resource):** Provisions a Cloudflare R2 bucket named "Bucket" for general storage purposes.
*   **`api` (resource):** Defines a Cloudflare Worker named "Api". This worker handles API requests, is linked to the `packages/function/src/api.ts` handler, and is configured with a specific domain, URL, and bindings (including a Durable Object namespace `SYNC_SERVER`). It also configures logpush and migration settings.
*   **`Web` (resource):** Defines an SST Cloudflare Astro site component named "Web". This component deploys the Astro application located in `packages/web`, associates it with the determined `domain`, and injects the API URL as an environment variable (`VITE_API_URL`).

## Important Variables/Constants

*   **`$app.stage` (SST global):** An SST-provided variable that indicates the current deployment stage (e.g., "production", "dev"). This is used to determine the correct domain and other stage-specific configurations.
*   **`SYNC_SERVER` (binding):** A configuration within the `api` worker that binds a Durable Object namespace with the class name `SyncServer`. This allows the API worker to interact with `SyncServer` Durable Objects.

## Usage Examples

This file is not directly used in application code but is processed by the SST CLI to deploy the infrastructure.

To deploy the infrastructure defined in this file, you would typically run an SST command like:
```bash
npx sst deploy --stage <stage_name>
```

## Dependencies and Interactions

*   **SST (Serverless Stack Toolkit):** The entire file is based on SST's constructs and types (e.g., `sst.cloudflare.Bucket`, `sst.cloudflare.Worker`, `sst.cloudflare.x.Astro`).
*   **`packages/function/src/api.ts`:** The `api` worker is configured to use this file as its handler. This means all API requests to `api.${domain}` will be processed by the code in `api.ts`.
*   **`packages/web`:** The `Web` component deploys the Astro application located in this directory.
*   **Cloudflare:** This file defines resources that will be provisioned on Cloudflare (Workers, R2, Domains).
*   **`SyncServer` Durable Object:** The API worker is configured to be able to create and interact with Durable Objects of the class `SyncServer` (defined in `packages/function/src/api.ts`).

---
*Generated by OpenCode AI Agent.*
